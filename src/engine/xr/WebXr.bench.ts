import { bench, describe } from 'vitest'; import { eyeView, perspective, planStereoViews } from './StereoViewPlanner'; const projection = perspective(Math.PI / 2, 1, .05, 100), views = [{ eye: 'left' as const, projection, view: eyeView(-.032) }, { eye: 'right' as const, projection, view: eyeView(.032) }]; describe('stereo view planning', () => { bench('plan 100,000 dual-view XR frames', () => { for (let i = 0; i < 100000; i++) planStereoViews(views, .8); }); });
