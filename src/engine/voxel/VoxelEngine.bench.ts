import{bench,describe}from'vitest';import{VoxelPlanet}from'./VoxelPlanet';import{generateSurfaceNet}from'./SurfaceNets';import{LodOctree}from'./LodOctree';const planet=new VoxelPlanet(11,48),tree=new LodOctree(512,5);describe('voxel geometry',()=>{bench('generate 32 cubed surface-net chunk',()=>{generateSurfaceNet(32,128,{x:-64,y:-64,z:-64},(x,y,z)=>planet.density(x,y,z));});bench('select planetary LOD octree',()=>{tree.select({x:12,y:30,z:420});});});
