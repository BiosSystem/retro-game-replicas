import{describe,expect,it}from'vitest';import type{VisualGraph}from'../../ui/studio/VisualGraph';import{instruction,NeonOpcode,NeonVM}from'./NeonVM';import{compileGraphBytecode}from'./VisualBytecodeCompiler';
describe('NeonVM',()=>{it('executes deterministic 16-bit arithmetic',()=>{const program=new Uint16Array([instruction(NeonOpcode.LOADI,0),300,instruction(NeonOpcode.LOADI,1),20,instruction(NeonOpcode.ADD,0,1),instruction(NeonOpcode.HALT)]);expect(new NeonVM().run(program).registers[0]).toBe(320);});it('halts loops through gas metering',()=>{const program=new Uint16Array([instruction(NeonOpcode.JUMP),0]);expect(()=>new NeonVM().run(program,undefined,20)).toThrow('gas exhausted');});it('compiles safe visual graphs to events',()=>{const graph:VisualGraph={nodes:[{id:'event-1',kind:'EVENT',x:0,y:0,config:{event:'SCORE_UPDATE'}},{id:'score-2',kind:'SCORE',x:1,y:1,config:{factor:2}}],edges:[{from:'event-1',to:'score-2'}]};const result=new NeonVM().run(compileGraphBytecode(graph),new Uint16Array([500]));expect(result.events).toContainEqual({code:10,value:1000});});});
