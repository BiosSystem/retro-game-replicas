import{describe,expect,it}from'vitest';import{createSwarmIdentity,ScoreGossip}from'./ScoreGossip';describe('score gossip',()=>{it('converges signed claims independent of order',async()=>{const keys=await createSwarmIdentity();const a=new ScoreGossip(),b=new ScoreGossip();const first=await a.create('AAA','LabyrinthScene',900,'a'.repeat(64),keys,1);const second=await a.create('BBB','LabyrinthScene',1200,'b'.repeat(64),keys,2);expect(await b.merge({type:'SCORE_GOSSIP',claims:[second,first]})).toBe(2);expect(b.top()[0].score).toBe(1200);expect(await a.merge(b.envelope())).toBe(0);});it('rejects modified scores',async()=>{const keys=await createSwarmIdentity();const a=new ScoreGossip(),b=new ScoreGossip();const claim=await a.create('AAA','LabyrinthScene',100,'c'.repeat(64),keys,1);claim.score=999;expect(await b.merge({type:'SCORE_GOSSIP',claims:[claim]})).toBe(0);});});
