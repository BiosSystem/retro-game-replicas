export interface ScoreClaim {version:1;id:string;player:string;game:string;score:number;replayHash:string;clock:number;publicKey:string;signature:string;}
export interface GossipEnvelope {type:'SCORE_GOSSIP';claims:ScoreClaim[];}
export class ScoreGossip extends EventTarget{private readonly claims=new Map<string,ScoreClaim>();async create(player:string,game:string,score:number,replayHash:string,keyPair:CryptoKeyPair,clock=Date.now()){const base={version:1 as const,id:`${game}-${clock}-${Math.floor(score)}`,player:safeId(player),game:safeId(game),score:clampInt(score,0,0x7fffffff),replayHash:/^[a-f0-9]{64}$/.test(replayHash)?replayHash:'0'.repeat(64),clock:clampInt(clock,0,Number.MAX_SAFE_INTEGER),publicKey:toBase64(new Uint8Array(await crypto.subtle.exportKey('raw',keyPair.publicKey)))};const signature=toBase64(new Uint8Array(await crypto.subtle.sign({name:'Ed25519'},keyPair.privateKey,new TextEncoder().encode(canonical(base)))));const claim={...base,signature};this.claims.set(claim.id,claim);return claim;}async merge(envelope:GossipEnvelope){if(envelope.type!=='SCORE_GOSSIP'||!Array.isArray(envelope.claims)||envelope.claims.length>256)return 0;let added=0;for(const claim of envelope.claims)if(!this.claims.has(claim.id)&&await verifyClaim(claim)){this.claims.set(claim.id,claim);added++;this.dispatchEvent(new CustomEvent('score',{detail:claim}));}return added;}envelope():GossipEnvelope{return{type:'SCORE_GOSSIP',claims:[...this.claims.values()].sort((a,b)=>a.id.localeCompare(b.id)).slice(-256)};}top(game?:string,limit=10){return[...this.claims.values()].filter(claim=>!game||claim.game===game).sort((a,b)=>b.score-a.score||a.id.localeCompare(b.id)).slice(0,Math.max(1,Math.min(100,limit)));}}
export async function createSwarmIdentity(){return crypto.subtle.generateKey({name:'Ed25519'},true,['sign','verify'])as Promise<CryptoKeyPair>;}
export async function verifyClaim(claim:ScoreClaim){try{if(claim.version!==1||safeId(claim.player)!==claim.player||safeId(claim.game)!==claim.game||!Number.isInteger(claim.score)||claim.score<0||claim.score>0x7fffffff||!Number.isSafeInteger(claim.clock)||!/^[a-f0-9]{64}$/.test(claim.replayHash)||claim.id.length>128)return false;const key=await crypto.subtle.importKey('raw',fromBase64(claim.publicKey),{name:'Ed25519'},false,['verify']);const{signature,...base}=claim;return crypto.subtle.verify({name:'Ed25519'},key,fromBase64(signature),new TextEncoder().encode(canonical(base)));}catch{return false;}}
function canonical(value:Omit<ScoreClaim,'signature'>){return JSON.stringify({clock:value.clock,game:value.game,id:value.id,player:value.player,publicKey:value.publicKey,replayHash:value.replayHash,score:value.score,version:1});}function safeId(value:string){const clean=String(value).replace(/[^A-Za-z0-9_-]/g,'').slice(0,32);return clean||'AAA';}function clampInt(value:number,min:number,max:number){return Math.max(min,Math.min(max,Math.floor(Number.isFinite(value)?value:min)));}function toBase64(bytes:Uint8Array){let binary='';for(const byte of bytes)binary+=String.fromCharCode(byte);return btoa(binary);}function fromBase64(value:string){const binary=atob(value);return Uint8Array.from(binary,char=>char.charCodeAt(0));}
