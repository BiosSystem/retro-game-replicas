import { DSP_WASM_BYTES,type DspWave } from './WasmDspCore';
export const AUDIO_RENDER_QUANTUM=128;
export class WorkletSynth{private node?:AudioWorkletNode;private readonly context:AudioContext;private readonly destination:AudioNode;constructor(context:AudioContext,destination:AudioNode){this.context=context;this.destination=destination;}async initialize(){if(this.node)return true;if(!this.context.audioWorklet||typeof AudioWorkletNode==='undefined')return false;const source=workletSource(DSP_WASM_BYTES),url=URL.createObjectURL(new Blob([source],{type:'text/javascript'}));try{await this.context.audioWorklet.addModule(url);this.node=new AudioWorkletNode(this.context,'bios-wasm-synth',{numberOfOutputs:1,outputChannelCount:[1]});this.node.connect(this.destination);return true;}finally{URL.revokeObjectURL(url);}}note(frequency:number,wave:DspWave='SQUARE',gain=.15){this.node?.port.postMessage({frequency:Math.max(20,Math.min(4000,frequency)),wave,gain:Math.max(0,Math.min(.5,gain))});}destroy(){this.node?.disconnect();this.node=undefined;}}
export function theoreticalQuantumMs(sampleRate:number){return AUDIO_RENDER_QUANTUM/Math.max(8000,Math.min(192000,sampleRate))*1000;}
function workletSource(bytes:Uint8Array){return `const B=new Uint8Array([${[...bytes].join(',')}]);class S extends AudioWorkletProcessor{constructor(){super();this.p=0;this.n=305419896;this.f=220;this.w='SQUARE';this.g=.15;this.add=null;WebAssembly.instantiate(B).then(m=>this.add=m.instance.exports.add);this.port.onmessage=e=>{this.f=e.data.frequency;this.w=e.data.wave;this.g=e.data.gain}}process(i,o){const c=o[0][0];for(let x=0;x<c.length;x++){this.p=this.add?this.add(this.p,this.f/sampleRate):this.p+this.f/sampleRate;if(this.p>=1)this.p-=1;let s;if(this.w==='TRIANGLE')s=1-4*Math.abs(this.p-.5);else if(this.w==='NOISE'){this.n^=this.n<<13;this.n^=this.n>>>17;this.n^=this.n<<5;s=(this.n|0)/2147483648}else s=this.p<.5?1:-1;c[x]=s*this.g}return true}}registerProcessor('bios-wasm-synth',S)`;}
