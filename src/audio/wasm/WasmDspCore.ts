export type DspWave='SQUARE'|'TRIANGLE'|'NOISE';
export const DSP_WASM_BYTES=Uint8Array.of(0,97,115,109,1,0,0,0,1,7,1,96,2,125,125,1,125,3,2,1,0,7,7,1,3,97,100,100,0,0,10,9,1,7,0,32,0,32,1,146,11);
type Add=(a:number,b:number)=>number;
export class WasmDspCore{private phase=0;private noise=0x12345678;private readonly add:Add;private constructor(add:Add){this.add=add;}static async create(){const module=await WebAssembly.instantiate(DSP_WASM_BYTES);return new WasmDspCore((module.instance.exports.add as Add));}render(output:Float32Array,frequency:number,sampleRate:number,wave:DspWave,gain=.2){const increment=Math.max(0,Math.min(.49,frequency/Math.max(8000,sampleRate)));for(let i=0;i<output.length;i++){this.phase=this.add(this.phase,increment);if(this.phase>=1)this.phase-=1;let sample=0;if(wave==='SQUARE')sample=this.phase<.5?1:-1;else if(wave==='TRIANGLE')sample=1-4*Math.abs(this.phase-.5);else{this.noise^=this.noise<<13;this.noise^=this.noise>>>17;this.noise^=this.noise<<5;sample=(this.noise|0)/2147483648;}output[i]=sample*Math.max(0,Math.min(1,gain));}return output;}getPhase(){return this.phase;}}
