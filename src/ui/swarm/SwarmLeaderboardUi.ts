import type{ScoreClaim}from'../../net/swarm/ScoreGossip';export class SwarmLeaderboardUi{private readonly root:HTMLElement;constructor(){this.root=document.createElement('aside');this.root.className='swarm-board';this.root.hidden=true;this.root.innerHTML='<header><strong>CONNECTED SWARM SCORES</strong><button data-close>X</button></header><ol data-list><li>NO VERIFIED PEER SCORES</li></ol><small>CONNECTED PEERS ONLY. NO GLOBAL DISCOVERY.</small>';document.body.appendChild(this.root);this.root.querySelector('[data-close]')?.addEventListener('click',()=>this.toggle(false));window.addEventListener('arcade-swarm-board',event=>this.render((event as CustomEvent<ScoreClaim[]>).detail));window.addEventListener('keydown',event=>{if(event.code==='KeyL'&&!(event.target as HTMLElement|null)?.matches('input,textarea,select'))this.toggle();});}toggle(force?:boolean){this.root.hidden=force===undefined?!this.root.hidden:!force;}private render(claims:ScoreClaim[]){const list=this.root.querySelector('[data-list]');if(!list)return;list.replaceChildren(...claims.slice(0,10).map(claim=>{const item=document.createElement('li');item.textContent=`${claim.player} ${claim.game} ${claim.score}`;return item;}));if(!claims.length){const item=document.createElement('li');item.textContent='NO VERIFIED PEER SCORES';list.appendChild(item);}}}export function installSwarmLeaderboard(){return new SwarmLeaderboardUi();}
