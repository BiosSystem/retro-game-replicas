import{describe,expect,it}from'vitest';import{initialKombatState,kombatChecksum,kombatInput,stepKombat}from'./KombatSystems';describe('Kombat frame data',()=>{it('applies startup, active, hit-stun, and combo scaling deterministically',()=>{let a=initialKombatState(),b=initialKombatState();a.p1.x=b.p1.x=300;a.p2.x=b.p2.x=340;for(let frame=1;frame<=20;frame++){const p1=kombatInput(frame,frame===1?16:0,0),p2=kombatInput(frame,0,0);a=stepKombat(a,p1,p2);b=stepKombat(b,p1,p2);}expect(a.p2.health).toBeLessThan(1000);expect(kombatChecksum(a)).toBe(kombatChecksum(b));expect(a.p2.comboDamage).toBeGreaterThan(0);});it('applies block-stun and chip damage while holding away',()=>{let state=initialKombatState();state.p1.x=300;state.p2.x=330;for(let frame=1;frame<=8;frame++)state=stepKombat(state,kombatInput(frame,frame===1?16:0,0),kombatInput(frame,0,127));expect(state.p2.blockstun).toBeGreaterThan(0);expect(state.p2.health).toBe(994);});});
