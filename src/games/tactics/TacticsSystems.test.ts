import { describe, expect, it } from 'vitest';
import { buildFlowField, dispatchUnits, findPathAStar, FLOW_FIELD_WGSL, fogMask, generateTacticsMap } from './TacticsSystems';
describe('tactics systems',()=>{it('generates deterministic maps and valid A-star paths',()=>{const a=generateTacticsMap(8),b=generateTacticsMap(8);expect([...a.terrain]).toEqual([...b.terrain]);const path=findPathAStar(a,{x:2,y:2},{x:a.width-3,y:a.height-3});expect(path.length).toBeGreaterThan(2);for(const point of path)expect(a.terrain[point.y*a.width+point.x]).toBe(0);});it('dispatches 10000 units through one deterministic flow field',()=>{const map=generateTacticsMap(9,64,64);const field=buildFlowField(map,{x:50,y:24});const units=new Float32Array(10000*4);for(let i=0;i<10000;i++){units[i*4]=2;units[i*4+1]=2;}expect(dispatchUnits(units,field.directions,map.width)).toBe(10000);expect(Math.abs(units[2])+Math.abs(units[3])).toBe(1);expect(FLOW_FIELD_WGSL).toContain('@workgroup_size(64)');});it('reveals bounded fog around friendly units',()=>{const map=generateTacticsMap();const units=new Float32Array([4,4,0,0]);const visible=fogMask(map,units,1,3);expect(visible[4*map.width+4]).toBe(1);expect(visible.at(-1)).toBe(0);});});
